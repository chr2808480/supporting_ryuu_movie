# 運営ガイド（サイト運営者向け）

「ryuu星軍 本部」を安全に・長く続けるための運用メモです。

## 1. 非公式サイトとして守ること

これは推しを守るためのルールでもあります。

- **常に「非公式」と明示する。** トップの警告バナー、`<title>`、フッターの 3 か所に入っています。消さないでください。
- **公式を装わない。** 本人のアイコン・ヘッダー・ロゴを流用しない。プロフィール画像やサムネイルを転載しない。
- **画像・動画を転載しない。** 埋め込みではなく、公式ページへの**リンク**で案内します（現状のリンクカード方式）。
- **収益化しない。** 広告・アフィリエイト・投げ銭の導線を置かない。置くならまず本人の許諾を。
- **個人情報を載せない。** 本名・居住地・所属・中の人に関する推測は、公開情報であっても扱わない。
- **削除依頼には即応する。** ご本人・権利者からの連絡は最優先。窓口（Issue など）を分かりやすく保つ。
- **原作の権利に配慮する。** 『ジョジョの奇妙な冒険』関連の画像・台詞の引用は最小限に。

もし本人に事前連絡できるなら、「非公式ファンサイトを作りました。問題があれば下げます」と一報を入れておくと、
お互いに安心して続けられます。

## 2. ファイル構成

```
.
├── index.html              # サイト本体（1 ページ完結）
├── assets/
│   ├── css/style.css       # 配色・レイアウト
│   └── js/main.js          # メニュー / カウントダウン / タグコピー
├── docs/
│   ├── research.md         # 調査メモと情報源（更新前にここを見る）
│   └── operations.md       # このファイル
└── README.md
```

ビルド不要の静的サイトです。`index.html` をブラウザで開けばそのまま確認できます。

ローカルで通して確認したいときは、リポジトリのルートで簡易サーバーを立ててください。

```bash
python3 -m http.server 8000
# → http://localhost:8000
```

## 3. 情報を更新するとき

1. `docs/research.md` の確度表を見る。⚠️ が付いている項目は必ず本人の導線から確認する。
2. `index.html` の該当箇所を直す。主な編集ポイントは次のとおり。
   - プロフィール → `#about` の `.datalist`
   - 公式リンク → `#links` の `.linkcard`（`href` と `.handle`）
   - 配信時刻 → `#schedule` の `.schedule-time`、および `assets/js/main.js` の `nextStream()`
     （**曜日と時刻は 2 か所**にあります。片方だけ直すとカウントダウンがずれます）
   - 活動の歩み → `#history` の `.timeline`
3. `#site` セクションと `docs/research.md` の「最終調査日 / 最終更新」を更新する。
4. コミットして push。

### 配信時刻を変更する場合

`assets/js/main.js` のカウントダウンは、端末のタイムゾーンに左右されないよう **UTC** で計算しています。

```js
Date.UTC(..., 4, 0, 0, 0)  // 04:00 UTC = 13:00 JST
var delta = (6 - d.getUTCDay() + 7) % 7;  // 6 = 土曜
```

日本時間から 9 時間引いた値を `Date.UTC` の時に、曜日を `6`（日=0 … 土=6）の位置に入れてください。
JST の時刻が 9:00 より前の場合は UTC では前日になるため、曜日も 1 つ戻す必要があります。

## 4. 公開する（GitHub Pages）

1. GitHub のリポジトリ → **Settings** → **Pages**
2. **Source** で `Deploy from a branch` を選ぶ
3. **Branch** に公開したいブランチ（例：`main`）と `/ (root)` を指定して **Save**
4. 数分後に `https://<ユーザー名>.github.io/supporting_ryuu_movie/` で公開されます

独自ドメインを使う場合はリポジトリ直下に `CNAME` ファイルを置き、Pages の設定で指定してください。

## 5. これから足せそうなもの

- おすすめ動画リスト（**埋め込みではなくリンクで**。何がどう良いかの一言コメント付き）
- 用語集（「空条穹太郎」「星軍」など、新規の人が置いていかれない辞書）
- ファンアートギャラリー — ただし**掲載は必ず描き手本人の許可を取ってから**。無断転載はファンサイトが最も燃えやすいポイントです
- 配信アーカイブの索引（タイトルと日付だけのテキスト索引なら安全）
- OGP 画像（ファン自作のイラスト／文字だけのバナー。本人の画像は使わない）
